class Particle {
    constructor(pos, radius, blur, intensity, volatility) {
        this.pos = pos;
        this.radius = radius;
        this.blur = blur;
        this.intensity = intensity;
        this.volatility = volatility;
    }
}